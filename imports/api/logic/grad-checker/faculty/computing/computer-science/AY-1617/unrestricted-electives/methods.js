
export const findUnrestrictedElectivesRequirementModules = function findUnrestrictedElectivesRequirementModules(allMarkedGradRequirements, studentPlanner, waivedModules) {
  // get total required MC to graduate

  // loop through all waivedModules
    // find number of MCs and add into total required MCs

  // loop through all marked grad requirements
    // loop through one marked grad requirements
      // find marked grad requirement module marked true
      // find the number of MCs and remove it from the total required MC
      // remove module from studentPlanner

  // check if number of of MCs equal to 0, if so, return true
}
